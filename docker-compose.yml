services:
  jekyll:
    image: jekyll/jekyll
    user: root
    environment:
      - MSYS_NO_PATHCONV=1
      - JEKYLL_VERSION=4.3.4
    volumes:
      - .:/srv/jekyll
    working_dir: /srv/jekyll
    tty: true
    command: jekyll serve --watch --incremental
    ports:
      - "4000:4000"
